<script setup lang="ts">
// Deprecated. The login background is now a pure-CSS liquid-gold light
// rendered directly in app/pages/login.vue (.login-aurora) so it paints on
// the first frame with zero flash and no WebGL dependency. This stub is kept
// only so any stale import resolves; it renders nothing.
</script>

<template>
  <div class="login-scene-deprecated" aria-hidden="true" hidden />
</template>
